# ─── DEV: HTTP simples ──────────────────────────────────────────
server {
    listen 80;
    server_name ~^(?<tenant>.+)\.dnsoftware\.com\.br$ dnsoftware.com.br localhost;

    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location /media/ {
        alias /app/media/;
        expires 7d;
    }

    location / {
        proxy_pass         http://web:8000;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }
}

# ─── PROD: HTTPS wildcard (descomente em produção) ──────────────
# server {
#     listen 80;
#     server_name ~^(?<tenant>.+)\.dnsoftware\.com\.br$ dnsoftware.com.br;
#     return 301 https://$host$request_uri;
# }
#
# server {
#     listen 443 ssl http2;
#     server_name ~^(?<tenant>.+)\.dnsoftware\.com\.br$ dnsoftware.com.br;
#
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#
#     location /static/ { alias /app/staticfiles/; expires 30d; }
#     location /media/  { alias /app/media/; expires 7d; }
#
#     location / {
#         proxy_pass         http://web:8000;
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto https;
#     }
# }
